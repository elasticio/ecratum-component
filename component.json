{
  "title": "ecratum",
  "description": "elastic.io integration component for ecratum API",
  "credentials": {
    "fields": {
      "token": {
        "label": "API Token",
        "required": true,
        "viewClass": "TextFieldView"
      },
      "baseURL": {
        "label": "Base URL",
        "required": true,
        "viewClass": "TextFieldView",
        "placeholder":"foo.ecratum.tk",
        "note": "Only domain name without 'http://' or slashes"
      }
    }
  }
}
